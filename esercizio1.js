//Scrivere una funzione che in input accetti un parametro x e restituisca il tipo della variabile x.

const x = "2";

const returnTypeDato = (dato) => {
  console.log(typeof dato);
};

returnTypeDato(x);
